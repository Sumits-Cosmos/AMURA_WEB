import dotenv from "dotenv"

dotenv.config();

const config = {
    port: process.env.PORT || 3000,
    db: process.env.DB || "MONGODB_URI = mongodb+srv://cosmophile_sumit:123Cosmos123_@cluster0.hthcrj2.mongodb.net/amura-tech-club",
    jwtSecret: process.env.JWT_SECRET
}

export default config;